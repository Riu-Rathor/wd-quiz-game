
# Ensure dependencies are installed from npm registry
registry=https://registry.npmjs.org/
# Enable exact package versions
save-exact=true
# Disable package-lock for cleaner builds
package-lock=false